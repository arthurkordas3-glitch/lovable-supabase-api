from setuptools import setup, find_packages

setup(
    name='my-package',
    version='0.1.0',
    description='My Python package',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
)
